# Cross-game shared skills

This directory holds skills that are shared across all games.
Examples:
- Common ADB operations (swipe, back)
- Device-level maintenance tasks
- Generic UI navigation patterns

Game-specific skills should go in `data/skills/<game>/`.
